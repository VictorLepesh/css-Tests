// Create an array

let fruits = ['pear', 'apple'];

// get length of array
console.log(fruits.length);

// Get last index of array
console.log(fruits.length - 1);

Array.prototype.customPropertyForAllArrays = "value";
console.log(fruits.customPropertyForAllArrays);