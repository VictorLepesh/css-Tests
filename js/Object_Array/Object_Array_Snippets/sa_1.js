// How to initialize
let arr = ['hunter', 'rhino'];
let arr1 = Array('buddy', 'boot', 'matt'); // Not sure how to set the arrayLength parameter


// JS Array's starting index is 0
console.log(arr[1]);

// To get the length
console.log(arr.length);

// to get second position 
console.log(arr.length - 1)

console.log(arr1);

let arr2 = Array(3); // doesn't limit length, just sets empty slots. 
console.log(arr2);
console.log(arr2.length)