let thing = Object('test') // takes 1 value, returns an ARRAY with an array of the value inside.
                            // If no value, returns an Object

console.log(thing);

let bang = Object()
console.log(bang);